services:
  # Base de datos PostgreSQL
  db:
    image: postgres:16
    container_name: postgres_example
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: laravel
    ports:
      - "5432:5432"

  # Aplicación Laravel con Nginx + PHP-FPM
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel_app
    restart: always
    volumes:
      - .:/var/www/html
      - /var/www/html/vendor          # ← CLAVE: volumen anónimo para vendor
      - /var/www/html/node_modules    # ← También para node_modules
    ports:
      - "8080:80"
    environment:
      # Configuración de desarrollo
      - APP_ENV=local
      - APP_DEBUG=true
      - LOG_CHANNEL=stderr
     
      # Conexión a PostgreSQL
      - DB_CONNECTION=pgsql
      - DB_HOST=db
      - DB_PORT=5432
      - DB_DATABASE=laravel
      - DB_USERNAME=user
      - DB_PASSWORD=1234
     
      # Configuración de la imagen
      - SKIP_COMPOSER=0
      - WEBROOT=/var/www/html/public
      - PHP_ERRORS_STDERR=1
      - RUN_SCRIPTS=1
      - REAL_IP_HEADER=1
      - COMPOSER_ALLOW_SUPERUSER=1
    depends_on:
      - db